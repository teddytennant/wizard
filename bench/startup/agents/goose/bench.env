CMD="goose session"
MARKER="Enter to send"
VERSION_CMD="goose --version"
BUNDLE='stat -c %s $(readlink -f $(command -v goose))'
NOTE="download_cli.sh with CONFIGURE=false. openai provider with OPENAI_HOST 127.0.0.1:9, telemetry opt-in pre-answered in config.yaml. Measured command is 'goose session' (bare 'goose' prints help)."
