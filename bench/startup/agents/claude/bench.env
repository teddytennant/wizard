CMD="claude"
MARKER="❯"
VERSION_CMD="claude --version"
BUNDLE='stat -c %s $(readlink -f $(command -v claude))'
NOTE="Native installer. Onboarding, API-key approval and folder trust pre-answered in ~/.claude.json; dummy ANTHROPIC_API_KEY, so its startup calls to api.anthropic.com get 401."
