FROM wizard-bench-base
# README: curl -LsSf https://aider.chat/install.sh | sh  (installs uv, then aider as a uv tool with its own python)
RUN curl -LsSf https://aider.chat/install.sh | sh
ENV PATH="/root/.local/bin:${PATH}"
