type: agent
name: nexau_code_agent
max_context_tokens: 200000
system_prompt: ./systemprompt.md
system_prompt_type: jinja
tool_call_mode: openai
max_iterations: 300

llm_config:
  model: ${env.LLM_MODEL}
  base_url: ${env.LLM_BASE_URL}
  api_key: ${env.LLM_API_KEY}
  max_tokens: 32000
  temperature: 0.7
  top_p: 0.95
  stream: true
  # openai = chat/completions (broadly compatible incl. local llama.cpp);
  # openai_responses = OpenAI Responses API. Set via LLM_API_TYPE.
  api_type: ${env.LLM_API_TYPE}

tools:
  # File operations
  # Shell
  - name: run_shell_command
    yaml_path: ./tool_descriptions/run_shell_command.tool.yaml
    binding: tools.shell_tools:run_shell_command

tracers:
  - import: nexau.archs.tracer.adapters.in_memory:InMemoryTracer
